# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Design & Theme Guidelines
- Use the **half-white / cream** color (`#F9F9F6`) as the default background for all pages, except for the login page which uses the vertical split layout.

